# レポート閲覧 Design

> **ステータス**: Phase 1 実装済み
> **作成日**: 2026-01-25
> **スコープ**: D1（レポートメイン）、D2（サマリー）

---

## Spec Reference

- **requirements.md**: [./requirements.md](./requirements.md)
- **仕様概要**: [.kiro/specs/仕様概要/経営会議レポート機能.md](../../仕様概要/経営会議レポート機能.md)
- **共通Contracts**: `packages/contracts/src/bff/meetings/`

---

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              UI Layer                                    │
│  apps/web/_v0_drop/meetings/management-meeting-report/                  │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ D1: ReportMainPage                                               │   │
│  │ ┌────────────────────────────────────────────────────────────┐   │   │
│  │ │ [Header] 会議情報 + ステータス変更                         │   │   │
│  │ ├────────────────────────────────────────────────────────────┤   │   │
│  │ │ [Tabs]                                                     │   │   │
│  │ │  サマリー | 部門報告 | 資料                                │   │   │
│  │ ├────────────────────────────────────────────────────────────┤   │   │
│  │ │ [Tab Content]                                              │   │   │
│  │ │  ┌──────────────────────────────────────────────────────┐  │   │   │
│  │ │  │ D2: SummaryDashboard                                 │  │   │   │
│  │ │  │ - 提出状況カード                                     │  │   │   │
│  │ │  │ - KPIカードグリッド                                  │  │   │   │
│  │ │  │ - 要注目事項リスト                                   │  │   │   │
│  │ │  │ - 部門別提出状況                                     │  │   │   │
│  │ │  └──────────────────────────────────────────────────────┘  │   │   │
│  │ └────────────────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────┬────────────────────────────────┘   │
│                                     │                                    │
│                               BffClient                                  │
│                                     │                                    │
└─────────────────────────────────────┼────────────────────────────────────┘
                                      │ HTTP (JSON)
                                      │ /api/bff/meetings/*
┌─────────────────────────────────────┼────────────────────────────────────┐
│                                BFF Layer                                 │
│                                                                          │
│  ManagementMeetingReportController                                       │
│    - GET  /bff/meetings/events/:id                                      │
│    - GET  /bff/meetings/events/:id/submission-status                    │
│    - GET  /bff/meetings/events/:id/kpi-cards                            │
│    - POST /bff/meetings/events/:id/status                               │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## BFF Specification

### Endpoints（本Spec対象）

| Method | Path | 説明 | Request DTO | Response DTO |
|--------|------|------|-------------|--------------|
| GET | `/bff/meetings/events/:id` | 会議イベント詳細 | - | MeetingEventDto |
| GET | `/bff/meetings/events/:id/submission-status` | 提出状況一覧 | - | SubmissionStatusSummaryDto |
| GET | `/bff/meetings/events/:id/kpi-cards` | KPIカード一覧 | ?departmentStableId | KpiCardListDto |
| POST | `/bff/meetings/events/:id/status` | ステータス変更 | UpdateMeetingEventStatusDto | MeetingEventDto |

### Error Policy

**選択: [x] Option A: Pass-through**

---

## Contracts Summary

### 使用するContracts

**配置先**: `packages/contracts/src/bff/meetings/`

#### Enums（本Spec関連）

```typescript
export type KpiCardStatus =
  | 'ON_TRACK'    // 順調（達成率100%以上）
  | 'WARNING'     // 注意（達成率90-99%）
  | 'AT_RISK';    // 要注意（達成率90%未満）
```

#### DTOs（本Spec関連）

| DTO | 用途 |
|-----|------|
| `MeetingEventDto` | 会議イベント詳細（ヘッダー表示用） |
| `SubmissionStatusDto` | 部門ごとの提出状況 |
| `SubmissionStatusSummaryDto` | 提出状況サマリー（items + summary） |
| `KpiCardDto` | KPIカード（1指標分） |
| `KpiCardListDto` | KPIカード一覧 |

---

## UI Implementation

### ディレクトリ構成

```
apps/web/_v0_drop/meetings/management-meeting-report/src/
├── components/
│   ├── report/
│   │   ├── report-main-page.tsx       # D1 メインページ（タブコンテナ）
│   │   ├── summary-dashboard.tsx      # D2 サマリーダッシュボード
│   │   ├── kpi-card.tsx               # KPIカードコンポーネント
│   │   └── index.ts
│   └── shared/
│       └── status-badge.tsx           # KpiCardStatusBadge
├── api/
│   └── ...
└── index.ts
```

### UIパターン適用

| 画面 | パターン | 参照 |
|------|----------|------|
| D1 | タブコンテナ | Tabs コンポーネント |
| D2 | ダッシュボードパターン（Dashboard Pattern） | v0プロンプト Pattern 3 |

### ダッシュボードパターン仕様（D2）

```
┌────────────────────────────────────────────────────────────┐
│ [Summary Stats]  4カラムグリッド                           │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐│
│ │ 📊 提出状況 │ │ ✓ 順調    │ │ ⏰ 注意    │ │ ⚠ 要注意  ││
│ │   40%      │ │   2       │ │   1       │ │   1       ││
│ │ [Progress] │ │           │ │           │ │           ││
│ │ 2/5部門    │ │達成率100%+│ │達成率90-99│ │達成率90%- ││
│ └────────────┘ └────────────┘ └────────────┘ └────────────┘│
├────────────────────────────────────────────────────────────┤
│ [KPI Cards]  4カラムグリッド                               │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐│
│ │ 売上高     │ │ 売上総利益 │ │ 営業利益   │ │ 新規顧客数 ││
│ │ [ON_TRACK] │ │ [WARNING]  │ │ [AT_RISK]  │ │ [ON_TRACK] ││
│ │            │ │            │ │            │ │            ││
│ │ 5億円      │ │ 1.9億円    │ │ 7,200万    │ │ 105件      ││
│ │ 予算: 5億  │ │ 予算: 2億  │ │ 予算: 8000万│ │ 予算: 100件││
│ │ ██████████ │ │ ████████░░ │ │ ███████░░░ │ │ ██████████ ││
│ │ 達成100%   │ │ 達成95%    │ │ 達成90%    │ │ 達成105%   ││
│ │ 差異: +0%  │ │ 差異: -5%  │ │ 差異: -10% │ │ 差異: +5%  ││
│ └────────────┘ └────────────┘ └────────────┘ └────────────┘│
├────────────────────────────────────────────────────────────┤
│ [Attention Items]  AT_RISK のKPIリスト                     │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ ⚠ 要注目項目                                         │   │
│ │ 達成率が90%を下回っているKPIがあります               │   │
│ │ ┌────────────────────────────────────────────────┐   │   │
│ │ │ 営業利益      予算差異: -10%      達成率: 90%  │   │   │
│ │ └────────────────────────────────────────────────┘   │   │
│ └──────────────────────────────────────────────────────┘   │
├────────────────────────────────────────────────────────────┤
│ [Submission Status Detail]  部門別一覧                     │
│ ┌──────────────────────────────────────────────────────┐   │
│ │ 👥 部門別提出状況                                    │   │
│ │ ┌─────────────────────────────────────────────────┐  │   │
│ │ │ ● 営業部       提出済    2026/06/10            │  │   │
│ │ │ ○ 開発部       作成中    -                     │  │   │
│ │ │ ○ 管理部       未着手    -                     │  │   │
│ │ │ ● X事業部      提出済    2026/06/08            │  │   │
│ │ │ ○ Y事業部      作成中    -                     │  │   │
│ │ └─────────────────────────────────────────────────┘  │   │
│ └──────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

### KPIカード仕様

```typescript
// KpiCardコンポーネントの表示ロジック
const formatValue = (value: number, formatType: string) => {
  if (formatType === 'currency') {
    if (value >= 100000000) return `${(value / 100000000).toFixed(1)}億円`;
    if (value >= 10000) return `${(value / 10000).toFixed(0)}万円`;
    return `${value.toLocaleString()}円`;
  }
  return `${value.toLocaleString()}${unit}`;
};

// ステータス判定
const getStatus = (achievementRate: number): KpiCardStatus => {
  if (achievementRate >= 100) return 'ON_TRACK';
  if (achievementRate >= 90) return 'WARNING';
  return 'AT_RISK';
};
```

---

## Data Model

### エンティティ参照

| テーブル | 用途 | 本Specでの使用 |
|----------|------|----------------|
| meeting_events | 会議イベント | 参照（ヘッダー表示） |
| meeting_submissions | 部門報告 | 参照（提出状況） |
| (外部)財務データ | KPI実績 | 参照（KPIカード） |

---

## Requirements Traceability

| 要件ID | 要件 | 設計セクション | 実装責務 |
|--------|------|----------------|----------|
| FR-1.1 | 会議情報表示 | UI report-main-page.tsx Header | UI |
| FR-1.3 | タブ切替 | UI report-main-page.tsx Tabs | UI |
| FR-2.1 | 提出状況サマリー | BFF GET /submission-status | BFF + UI |
| FR-2.2 | KPIカード表示 | BFF GET /kpi-cards + UI kpi-card.tsx | BFF + UI |
| FR-2.5 | 達成状況色分け | UI KpiCardStatusBadge | UI |
| FR-2.6 | 要注目事項リスト | UI summary-dashboard.tsx | UI |

---

## 変更履歴

| 日付 | 変更内容 |
|------|----------|
| 2026-01-25 | 初版作成（Phase 1スコープ） |
| 2026-01-25 | Spec分割により独立化 |
