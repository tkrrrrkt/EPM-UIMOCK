# 経営会議レポート機能 仕様検討

> **検討日**: 2026-01-15
> **ステータス**: 検討中
> **関連**: レポート機能全般、KPI管理、アクションプラン管理

---

## 1. 概要

### 1.1 目的

経営会議（月次経営会議、取締役会等）の準備・実施・記録を支援する機能。
単なるBIレポートではなく、**会議運営プラットフォーム**として設計する。

### 1.2 ターゲット会議体

| 会議体 | 頻度 | 特徴 |
|--------|------|------|
| 月次経営会議 | 月1回 | メインターゲット |
| 取締役会 | 月/四半期 | 非上場は経営会議と統合も |
| 事業部内会議 | 隔週など | 見込管理、部門内レビュー |

### 1.3 会議の「問い」の流れ

```
Q1. 実績はどうだった？
    └─ 売上・粗利・営業利益の着地

Q2. 見込とどうずれた？
    └─ 先月見込 vs 当月実績の差異

Q3. なぜずれた？（差異要因）
    └─ 売上要因 → 粗利要因 → 販管費要因 → 利益インパクト
    └─ どの部門が、どの科目が、どれくらい

Q4. 着地見通しはどうなる？
    └─ 先月見込 vs 今月見込の変化
    └─ 当期着地（年度末）への影響

Q5. 大きいところはどこ？
    └─ 差異TOP、悪化TOP、リスクTOP

Q6. どうするのか？（アクション）
    └─ 定量：数値目標・リカバリープラン
    └─ 定性：施策・対応方針

Q7. 各部門の状況は？
    └─ 部門からの報告（登録制）
    └─ コメント・補足情報
```

---

## 2. 比較軸の優先順位

| 比較パターン | 用途 | 対象 |
|--------------|------|------|
| ① 予算 vs 実績 vs 見込 | 予算達成度＋着地見通し | 半期/通期レビュー |
| ② 前月見込 vs 今月見込 | 見通しの変化（1ヶ月推移） | 月次レビュー |
| ③ 先月登録見込 vs 当月実績 | 見込精度確認 | 月次レビュー |
| ④ 前月/前年/前期比較 | 必要に応じてオプション | 任意 |

---

## 3. 会議運営フロー

```
1. 会議設定（企画部門）
   └─ 会議種別・対象期間・締切日を設定

2. 部門報告登録（各部門）
   └─ 締切までに報告を登録
   └─ 見込入力のコメントを引用可能

3. 締切・確認（企画部門）
   └─ 締切後に内容確認・取りまとめ

4. 事前展開（役員・監査役）
   └─ 会議前に資料を展開・事前確認

5. 会議実施
   └─ ダッシュボード表示
   └─ 数値＋コメント＋KPI状況を確認

6. 会議後記録
   └─ 決定事項・アクションの記録
   └─ スナップショット保存（バッチで自動取得）
```

---

## 4. 報告の階層構造

```
DEPARTMENT レベル（各部門が登録）
├─ A部の報告
├─ B部の報告
└─ C部の報告
         │
         │ 参照・引用
         ▼
BU レベル（事業部長が登録）
└─ X事業部の報告
    ├─ 下位報告を参照しながらサマリー作成
    └─ 事業部特化情報を追記
         │
         │ 参照・引用
         ▼
COMPANY レベル（全社）
└─ 全社報告
    └─ 各事業部報告を集約表示
```

---

## 5. エンティティ設計

### 5.1 エンティティ一覧

| # | テーブル名 | 種別 | 説明 |
|---|------------|------|------|
| 1 | meeting_types | マスタ | 会議種別定義 |
| 2 | meeting_form_sections | マスタ | フォームセクション定義 |
| 3 | meeting_form_fields | マスタ | フォーム項目定義 |
| 4 | meeting_report_layouts | マスタ | レポートレイアウト定義 |
| 5 | meeting_report_pages | マスタ | レポートページ定義 |
| 6 | meeting_report_components | マスタ | レポートコンポーネント定義 |
| 7 | meeting_events | トランザクション | 会議イベント |
| 8 | meeting_submissions | トランザクション | 部門報告 |
| 9 | meeting_submission_values | トランザクション | 報告項目値 |
| 10 | meeting_attachments | トランザクション | 添付ファイル |
| 11 | meeting_snapshots | トランザクション | スナップショット |
| 12 | meeting_actions | トランザクション | アクションアイテム |
| 13 | plan_version_comments | トランザクション | 見込バージョンコメント（新規） |

---

### 5.2 meeting_types（会議種別マスタ）

#### 仕様
- 会議の種類を定義（月次経営会議、取締役会、事業部レビュー等）
- 会社単位で管理
- 全社オフィシャル or 部門ローカルを区分
- どの部門配下の会議か、どの階層まで報告を求めるかを設定

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| company_id | uuid | NO | | FK to companies |
| type_code | varchar(50) | NO | "MONTHLY_MGMT" | 会社内一意 |
| type_name | varchar(200) | NO | "月次経営会議" | |
| type_name_short | varchar(100) | YES | "月次会議" | |
| scope | varchar(20) | NO | "COMPANY" | COMPANY/LOCAL |
| scope_department_stable_id | varchar(50) | YES | | LOCAL時の対象部門（この配下が対象） |
| submission_depth | int | YES | 2 | 報告を求める階層深さ（1=直下のみ、NULL=全階層） |
| submission_levels | varchar[] | YES | {"DEPARTMENT","BU"} | 報告を求めるレベル |
| cycle | varchar(20) | NO | "MONTHLY" | MONTHLY/BIWEEKLY/QUARTERLY/YEARLY/AD_HOC |
| default_report_layout_id | uuid | YES | | デフォルトレポートレイアウト |
| submission_required | boolean | NO | true | 部門報告登録が必要か |
| link_to_plan_event | boolean | NO | false | 見込イベントと紐付けるか |
| description | text | YES | | |
| is_active | boolean | NO | true | |
| sort_order | int | NO | 10 | |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |
| created_by | uuid | YES | | 監査 |
| updated_by | uuid | YES | | 監査 |

#### 制約
- UNIQUE(tenant_id, company_id, type_code)
- FK(tenant_id, company_id) → companies(tenant_id, id)
- CHECK(scope IN ('COMPANY', 'LOCAL'))
- CHECK(cycle IN ('MONTHLY', 'BIWEEKLY', 'QUARTERLY', 'YEARLY', 'AD_HOC'))

#### 設定例

```
【例1: 全社経営会議】
scope: COMPANY
scope_department_stable_id: NULL  ← 全社
submission_depth: NULL            ← 全階層
submission_levels: ["DEPARTMENT", "BU"]

【例2: X事業部内会議】
scope: LOCAL
scope_department_stable_id: "DEPT-X-BU"  ← X事業部配下
submission_depth: 1                       ← 直下の部門のみ
submission_levels: ["DEPARTMENT"]

【例3: 営業本部会議】
scope: LOCAL
scope_department_stable_id: "DEPT-SALES-HQ"  ← 営業本部配下
submission_depth: 2                          ← 孫（課）まで
submission_levels: ["DEPARTMENT"]
```

---

### 5.3 meeting_form_sections（フォームセクション定義）

#### 仕様
- 会議種別ごとの報告フォームのセクション（大項目）を定義
- セクション単位で「部門ごとに入力」か「全社で1つ」かを制御

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| meeting_type_id | uuid | NO | | FK to meeting_types |
| section_code | varchar(50) | NO | "SALES_REPORT" | 会議種別内一意 |
| section_name | varchar(200) | NO | "売上報告" | |
| input_scope | varchar(20) | NO | "DEPARTMENT" | DEPARTMENT/BU/COMPANY |
| is_required | boolean | NO | true | 入力必須か |
| sort_order | int | NO | 1 | |
| description | text | YES | | セクションの説明 |
| is_active | boolean | NO | true | |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |

#### 制約
- UNIQUE(tenant_id, meeting_type_id, section_code)
- FK(tenant_id, meeting_type_id) → meeting_types(tenant_id, id)
- CHECK(input_scope IN ('DEPARTMENT', 'BU', 'COMPANY'))

---

### 5.4 meeting_form_fields（フォーム項目定義）

#### 仕様
- セクション内の入力項目を定義
- 項目タイプ（テキスト、数値、選択肢、ファイル等）を柔軟に設定

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| section_id | uuid | NO | | FK to meeting_form_sections |
| field_code | varchar(50) | NO | "variance_reason" | セクション内一意 |
| field_name | varchar(200) | NO | "差異要因" | |
| field_type | varchar(20) | NO | "TEXT" | 後述 |
| is_required | boolean | NO | true | |
| placeholder | varchar(200) | YES | "差異の主な要因を記入" | |
| options_json | jsonb | YES | | SELECT/MULTI_SELECT時の選択肢 |
| validation_json | jsonb | YES | | 入力検証ルール |
| default_value | text | YES | | デフォルト値 |
| max_length | int | YES | 1000 | TEXT/TEXTAREA時の最大文字数 |
| sort_order | int | NO | 1 | |
| help_text | text | YES | | 入力ヘルプ |
| is_active | boolean | NO | true | |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |

#### field_type 一覧

| タイプ | 説明 | 用途 |
|--------|------|------|
| TEXT | 短いテキスト | 一行コメント |
| TEXTAREA | 長いテキスト | 詳細説明、引用挿入対応 |
| NUMBER | 数値 | 定量データ |
| SELECT | 単一選択 | 見通し（好調/懸念等） |
| MULTI_SELECT | 複数選択 | 該当項目チェック |
| DATE | 日付 | 期限等 |
| FILE | ファイル添付 | Excel、PDF等 |
| FORECAST_QUOTE | 見込コメント引用 | 見込入力のコメントを引用 |

#### 制約
- UNIQUE(tenant_id, section_id, field_code)
- FK(tenant_id, section_id) → meeting_form_sections(tenant_id, id)
- CHECK(field_type IN ('TEXT', 'TEXTAREA', 'NUMBER', 'SELECT', 'MULTI_SELECT', 'DATE', 'FILE', 'FORECAST_QUOTE'))

---

### 5.5 meeting_report_layouts（レポートレイアウト定義）

#### 仕様
- 会議種別ごとのレポート表示レイアウトを定義
- 複数レイアウトを持てる（標準版、簡易版など）

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| meeting_type_id | uuid | NO | | FK to meeting_types |
| layout_code | varchar(50) | NO | "STANDARD" | 会議種別内一意 |
| layout_name | varchar(200) | NO | "標準レイアウト" | |
| description | text | YES | | |
| is_default | boolean | NO | false | デフォルトレイアウト |
| is_active | boolean | NO | true | |
| sort_order | int | NO | 10 | |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |
| created_by | uuid | YES | | 監査 |
| updated_by | uuid | YES | | 監査 |

#### 制約
- UNIQUE(tenant_id, meeting_type_id, layout_code)
- FK(tenant_id, meeting_type_id) → meeting_types(tenant_id, id)

---

### 5.6 meeting_report_pages（レポートページ定義）

#### 仕様
- レイアウト内のページ（タブ）を定義
- ページ種別で「固定1ページ」か「部門ごとに展開」かを制御

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| layout_id | uuid | NO | | FK to meeting_report_layouts |
| page_code | varchar(50) | NO | "EXEC_SUMMARY" | レイアウト内一意 |
| page_name | varchar(200) | NO | "エグゼクティブサマリー" | |
| page_type | varchar(20) | NO | "FIXED" | FIXED/PER_DEPARTMENT/PER_BU |
| expand_dimension_id | uuid | YES | | PER_BU時の展開軸 |
| sort_order | int | NO | 1 | タブ順序 |
| is_active | boolean | NO | true | |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |

#### 制約
- UNIQUE(tenant_id, layout_id, page_code)
- FK(tenant_id, layout_id) → meeting_report_layouts(tenant_id, id)
- FK(tenant_id, expand_dimension_id) → dimensions(tenant_id, id)
- CHECK(page_type IN ('FIXED', 'PER_DEPARTMENT', 'PER_BU'))

---

### 5.7 meeting_report_components（レポートコンポーネント定義）

#### 仕様
- ページ内に配置するコンポーネント（表、図、カード等）を定義
- 既存レポートへのリンクもコンポーネントとして配置可能

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| page_id | uuid | NO | | FK to meeting_report_pages |
| component_code | varchar(50) | NO | "kpi_cards" | ページ内一意 |
| component_name | varchar(200) | NO | "主要KPIカード" | |
| component_type | varchar(30) | NO | "KPI_CARD" | 後述 |
| data_source | varchar(30) | NO | "FACT" | FACT/KPI/SUBMISSION/SNAPSHOT/EXTERNAL |
| config_json | jsonb | NO | {} | コンポーネント設定 |
| width | varchar(10) | NO | "FULL" | FULL/HALF/THIRD |
| height | varchar(10) | YES | "AUTO" | AUTO/SMALL/MEDIUM/LARGE |
| sort_order | int | NO | 1 | 表示順序 |
| is_active | boolean | NO | true | |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |

#### component_type 一覧

| タイプ | 説明 | data_source |
|--------|------|-------------|
| KPI_CARD | 主要指標カード | FACT |
| TABLE | データ表（予実対比等） | FACT |
| CHART | グラフ（ウォーターフォール等） | FACT |
| SUBMISSION_DISPLAY | 部門報告表示 | SUBMISSION |
| REPORT_LINK | 既存レポートへのリンク | EXTERNAL |
| ACTION_LIST | アクション一覧 | EXTERNAL |
| SNAPSHOT_COMPARE | 前回会議との比較 | SNAPSHOT |
| KPI_DASHBOARD | KPI一覧ダッシュボード | KPI |
| AP_PROGRESS | アクションプラン進捗 | EXTERNAL |

#### 制約
- UNIQUE(tenant_id, page_id, component_code)
- FK(tenant_id, page_id) → meeting_report_pages(tenant_id, id)
- CHECK(component_type IN ('KPI_CARD', 'TABLE', 'CHART', 'SUBMISSION_DISPLAY', 'REPORT_LINK', 'ACTION_LIST', 'SNAPSHOT_COMPARE', 'KPI_DASHBOARD', 'AP_PROGRESS'))
- CHECK(data_source IN ('FACT', 'KPI', 'SUBMISSION', 'SNAPSHOT', 'EXTERNAL'))
- CHECK(width IN ('FULL', 'HALF', 'THIRD'))

---

### 5.8 meeting_events（会議イベント）

#### 仕様
- 実際に開催される会議のインスタンス
- 会議ステータスのライフサイクル管理
- 見込イベントとの紐付け（任意）
- **議事録の登録**（会議終了後に全体の結果・決定事項を記録）

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| company_id | uuid | NO | | FK to companies |
| meeting_type_id | uuid | NO | | FK to meeting_types |
| event_code | varchar(50) | NO | "MTG_202606" | 会社内一意 |
| event_name | varchar(200) | NO | "6月度経営会議" | |
| target_period_id | uuid | YES | | FK to accounting_periods（対象月） |
| target_fiscal_year | int | YES | 2026 | 対象年度 |
| plan_event_id | uuid | YES | | FK to plan_events（見込イベント紐付け） |
| status | varchar(20) | NO | "OPEN" | ステータス |
| submission_deadline | date | YES | | 報告登録締切日 |
| distribution_date | date | YES | | 事前展開日 |
| meeting_date | date | YES | | 会議開催日 |
| report_layout_id | uuid | YES | | 使用するレポートレイアウト |
| notes | text | YES | | |
| **minutes_text** | text | YES | | **議事録本文（リッチテキスト可）** |
| **minutes_recorded_at** | timestamptz | YES | | **議事録記録日時** |
| **minutes_recorded_by** | uuid | YES | | **議事録記録者** |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |
| created_by | uuid | YES | | 監査 |
| updated_by | uuid | YES | | 監査 |

#### ステータス遷移

```
DRAFT（下書き）
  ↓ 公開
OPEN（受付中）
  ↓ 締切到来 or 手動締切
COLLECTING（確認中）
  ↓ 事前展開
DISTRIBUTED（展開済）
  ↓ 会議実施
HELD（実施済）
  ↓ 記録完了
CLOSED（クローズ）
  ↓ アーカイブ
ARCHIVED（保管）
```

#### 制約
- UNIQUE(tenant_id, company_id, event_code)
- FK(tenant_id, company_id) → companies(tenant_id, id)
- FK(tenant_id, meeting_type_id) → meeting_types(tenant_id, id)
- FK(tenant_id, company_id, target_period_id) → accounting_periods(tenant_id, company_id, id)
- FK(tenant_id, company_id, plan_event_id) → plan_events(tenant_id, company_id, id)
- FK(tenant_id, report_layout_id) → meeting_report_layouts(tenant_id, id)
- CHECK(status IN ('DRAFT', 'OPEN', 'COLLECTING', 'DISTRIBUTED', 'HELD', 'CLOSED', 'ARCHIVED'))

---

### 5.9 meeting_submissions（部門報告）

#### 仕様
- 部門からの報告登録のヘッダー
- 階層型報告（DEPARTMENT→BU→COMPANY）をサポート

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| meeting_event_id | uuid | NO | | FK to meeting_events |
| submission_level | varchar(20) | NO | "DEPARTMENT" | DEPARTMENT/BU/COMPANY |
| department_stable_id | varchar(50) | YES | | 対象部門 |
| status | varchar(20) | NO | "DRAFT" | DRAFT/SUBMITTED |
| submitted_at | timestamptz | YES | | 提出日時 |
| submitted_by | uuid | YES | | 提出者 |
| notes | text | YES | | |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |
| created_by | uuid | YES | | 監査 |
| updated_by | uuid | YES | | 監査 |

#### 制約
- UNIQUE(tenant_id, meeting_event_id, submission_level, department_stable_id)
- FK(tenant_id, meeting_event_id) → meeting_events(tenant_id, id)
- CHECK(submission_level IN ('DEPARTMENT', 'BU', 'COMPANY'))
- CHECK(status IN ('DRAFT', 'SUBMITTED'))

---

### 5.10 meeting_submission_values（報告項目値）

#### 仕様
- 報告フォームの各項目に入力された値を保存
- 引用メタデータも保持（パターンB方式）

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| submission_id | uuid | NO | | FK to meeting_submissions |
| field_id | uuid | NO | | FK to meeting_form_fields |
| value_text | text | YES | | TEXT/TEXTAREA/SELECT値 |
| value_number | numeric | YES | | NUMBER値 |
| value_date | date | YES | | DATE値 |
| value_json | jsonb | YES | | MULTI_SELECT等 |
| quote_refs_json | jsonb | YES | | 引用メタデータ |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |

#### quote_refs_json の例

```json
[
  {
    "source_type": "SUBMISSION",
    "source_submission_id": "uuid-of-a-dept-submission",
    "source_field_id": "uuid-of-field",
    "quoted_at": "2026-06-09T10:30:00Z"
  },
  {
    "source_type": "PLAN_VERSION_COMMENT",
    "source_plan_version_id": "uuid-of-plan-version",
    "source_department_stable_id": "DEPT-001",
    "quoted_at": "2026-06-09T10:35:00Z"
  }
]
```

#### 制約
- UNIQUE(tenant_id, submission_id, field_id)
- FK(tenant_id, submission_id) → meeting_submissions(tenant_id, id)
- FK(tenant_id, field_id) → meeting_form_fields(tenant_id, id)

---

### 5.11 meeting_attachments（添付ファイル）

#### 仕様
- 報告に添付されたファイルを管理
- **議事録の添付ファイルも管理**（attachment_type = 'MINUTES'）

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| **attachment_type** | varchar(20) | NO | "SUBMISSION" | **SUBMISSION（部門報告）/MINUTES（議事録）** |
| **meeting_event_id** | uuid | YES | | **FK to meeting_events（議事録添付時）** |
| submission_id | uuid | YES | | FK to meeting_submissions（報告添付時） |
| field_id | uuid | YES | | FK to meeting_form_fields |
| file_name | varchar(255) | NO | "詳細分析.xlsx" | 元ファイル名 |
| file_path | varchar(500) | NO | | ストレージパス |
| file_size | bigint | NO | | バイト数 |
| mime_type | varchar(100) | NO | | |
| uploaded_at | timestamptz | NO | | |
| uploaded_by | uuid | NO | | |
| created_at | timestamptz | NO | | |

#### 制約
- FK(tenant_id, meeting_event_id) → meeting_events(tenant_id, id)
- FK(tenant_id, submission_id) → meeting_submissions(tenant_id, id)
- FK(tenant_id, field_id) → meeting_form_fields(tenant_id, id)
- **CHECK(attachment_type IN ('SUBMISSION', 'MINUTES'))**
- **CHECK((attachment_type = 'SUBMISSION' AND submission_id IS NOT NULL) OR (attachment_type = 'MINUTES' AND meeting_event_id IS NOT NULL))**

---

### 5.12 meeting_snapshots（スナップショット）

#### 仕様
- 会議クローズ時の数値・KPI・AP進捗をJSON保存（DB軽量化のため）
- 前回会議との比較に使用
- 会議終了後にバッチで自動取得

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| meeting_event_id | uuid | NO | | FK to meeting_events |
| snapshot_type | varchar(30) | NO | "FACT_SUMMARY" | FACT_SUMMARY/KPI_STATUS/ACTION_PROGRESS |
| captured_at | timestamptz | NO | | 取得日時 |
| data_json | jsonb | NO | | スナップショットデータ |
| created_at | timestamptz | NO | | |

#### snapshot_type 一覧

| タイプ | 内容 |
|--------|------|
| FACT_SUMMARY | 予算/実績/見込の集計結果 |
| KPI_STATUS | KPI達成状況 |
| ACTION_PROGRESS | アクションプラン進捗 |

#### data_json の例（FACT_SUMMARY）

```json
{
  "period": "FY2026-P03",
  "comparison_mode": "BUDGET_VS_ACTUAL_FORECAST",
  "summary": {
    "SALES": { "budget": 1200, "actual": 580, "forecast": 950, "variance": -250 },
    "GROSS_PROFIT": { "budget": 400, "actual": 200, "forecast": 310, "variance": -90 },
    "OP_INCOME": { "budget": 150, "actual": 80, "forecast": 110, "variance": -40 }
  },
  "by_organization": [
    { "org_id": "...", "org_name": "X事業部", "sales": {...}, "gross_profit": {...} }
  ]
}
```

#### 制約
- UNIQUE(tenant_id, meeting_event_id, snapshot_type)
- FK(tenant_id, meeting_event_id) → meeting_events(tenant_id, id)
- CHECK(snapshot_type IN ('FACT_SUMMARY', 'KPI_STATUS', 'ACTION_PROGRESS'))

---

### 5.13 meeting_actions（アクションアイテム）

#### 仕様
- 会議で決定したアクションを記録
- KPI、アクションプラン、WBS、タスクへのリンク可能

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| meeting_event_id | uuid | NO | | FK to meeting_events |
| action_no | int | NO | 1 | 会議内連番 |
| action_title | varchar(200) | NO | "A事業部売上リカバリー施策" | |
| action_detail | text | YES | | 詳細説明 |
| owner_department_stable_id | varchar(50) | YES | | 担当部門 |
| owner_employee_id | uuid | YES | | 担当者 |
| due_date | date | YES | | 期限 |
| status | varchar(20) | NO | "OPEN" | OPEN/IN_PROGRESS/COMPLETED/CANCELLED |
| linked_kpi_subject_id | uuid | YES | | FK to subjects（KPI科目） |
| linked_action_plan_id | uuid | YES | | FK to action_plans |
| linked_wbs_item_id | uuid | YES | | FK to wbs_items |
| linked_task_id | uuid | YES | | FK to action_plan_tasks |
| completed_at | timestamptz | YES | | 完了日時 |
| notes | text | YES | | |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |
| created_by | uuid | YES | | 監査 |
| updated_by | uuid | YES | | 監査 |

#### 制約
- UNIQUE(tenant_id, meeting_event_id, action_no)
- FK(tenant_id, meeting_event_id) → meeting_events(tenant_id, id)
- FK(tenant_id, owner_employee_id) → employees(tenant_id, id)
- FK(tenant_id, linked_kpi_subject_id) → subjects(tenant_id, id)
- FK(tenant_id, linked_action_plan_id) → action_plans(tenant_id, id)
- FK(tenant_id, linked_wbs_item_id) → wbs_items(tenant_id, id)
- FK(tenant_id, linked_task_id) → action_plan_tasks(tenant_id, id)
- CHECK(status IN ('OPEN', 'IN_PROGRESS', 'COMPLETED', 'CANCELLED'))

---

### 5.14 plan_version_comments（見込バージョンコメント）※新規

#### 仕様
- 見込入力時に部門単位でコメントを登録
- 会議報告での引用元となる

#### エンティティ

| カラム | 型 | NULL | 例 | 補足 |
|--------|---|------|-----|------|
| id | uuid | NO | | PK |
| tenant_id | uuid | NO | | RLS |
| company_id | uuid | NO | | FK to companies |
| plan_version_id | uuid | NO | | FK to plan_versions |
| department_stable_id | varchar(50) | NO | | 対象部門 |
| comment_text | text | NO | | コメント本文 |
| created_at | timestamptz | NO | | |
| updated_at | timestamptz | NO | | |
| created_by | uuid | YES | | 監査 |
| updated_by | uuid | YES | | 監査 |

#### 制約
- UNIQUE(tenant_id, plan_version_id, department_stable_id)
- FK(tenant_id, company_id) → companies(tenant_id, id)
- FK(tenant_id, company_id, plan_version_id) → plan_versions(tenant_id, company_id, id)

---

## 6. 画面一覧

### 6.1 マスタ管理系

| # | 画面名 | 概要 |
|---|--------|------|
| A1 | 会議種別一覧 | 登録済み会議種別の一覧、新規作成、編集、削除 |
| A2 | 会議種別設定 | 基本情報、スコープ設定、報告レベル設定 |
| A3 | 報告フォーム設定 | セクション追加、項目追加、並び替え |
| A4 | レポートレイアウト設定 | ページ追加、コンポーネント配置 |

### 6.2 会議運営系

| # | 画面名 | 概要 |
|---|--------|------|
| B1 | 会議イベント一覧 | 会議イベントの一覧、フィルタ、新規作成 |
| B2 | 会議イベント作成 | 会議種別選択、日程設定、対象期間設定 |
| B3 | 登録状況管理 | 提出状況一覧、催促、締切管理 |
| B4 | 会議クローズ | スナップショット取得、ステータス変更 |
| **B5** | **議事録登録** | **議事録本文の編集、添付ファイルアップロード、共有** |

### 6.3 報告登録系

| # | 画面名 | 概要 |
|---|--------|------|
| C1 | 報告登録（部門用） | フォーム入力、下書き保存、提出 |
| C2 | 報告登録（事業部用） | 下位報告参照、引用、サマリー入力 |
| C3 | 報告確認 | 提出済み報告の閲覧、編集（締切前のみ） |

### 6.4 レポート閲覧系

| # | 画面名 | 概要 |
|---|--------|------|
| D1 | 経営会議レポート | 会議用レポートのメイン画面、タブ切替 |
| D2 | エグゼクティブサマリー | KPIカード、ウォーターフォール、要注目事項 |
| D3 | 詳細分析 | 既存レポートへの遷移（予実対比、差異分析等） |
| D4 | 部門報告閲覧 | 階層表示、コメント閲覧 |
| D5 | KPI・アクション | KPIカード一覧、AP進捗、遷移リンク |
| D6 | アクション管理 | 会議アクションの一覧、進捗更新 |
| D7 | 前回比較 | スナップショット比較、差分表示 |
| **D8** | **議事録閲覧** | **議事録本文、添付ファイルダウンロード、過去会議検索** |

---

## 7. 既存機能との連携

### 7.1 連携マップ

```
                    ┌─────────────────────────────────┐
                    │       経営会議機能              │
                    └─────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│   数値データ   │    │   見込管理    │    │  KPI/AP管理   │
│ fact_amounts  │    │ plan_events   │    │ action_plans  │
│ subjects      │    │ plan_versions │    │ wbs_items     │
│ departments   │    │ plan_version_ │    │ tasks         │
│               │    │   comments    │    │               │
└───────────────┘    └───────────────┘    └───────────────┘
```

### 7.2 連携詳細

| 連携先 | 連携方法 | 用途 |
|--------|----------|------|
| fact_amounts | データ取得 | レポートの数値表示 |
| subjects | データ取得 | 科目名、集計構造 |
| departments | データ取得 | 部門名、階層構造 |
| plan_events/versions | データ取得 | 見込バージョンの特定 |
| plan_version_comments | 引用 | 見込コメントの引用 |
| subjects (type=KPI) | リンク・表示 | KPIダッシュボード |
| action_plans | リンク・新規作成 | APへの遷移、会議アクションからAP作成 |
| wbs_items | リンク | ガントチャートへの遷移 |
| action_plan_tasks | リンク | カンバンへの遷移 |

---

## 8. 既存レポートの組み込み

### 8.1 対象レポート

| # | レポート名 | 用途 | 組み込み方法 |
|---|------------|------|--------------|
| 1 | 予算実績照査 | 予実対比の基本 | リンク遷移 |
| 2 | 差異分析レポート | 差異の要因分析 | サマリー表示＋リンク遷移 |
| 3 | 確度別売上見込 | 案件の確度管理 | リンク遷移 |
| 4 | 予算消化推移 | 予算の消化状況 | サマリー表示＋リンク遷移 |
| 5 | シナリオ分析 | W/N/Bシナリオ比較 | リンク遷移 |

### 8.2 表示方式

- **ハイブリッド型**: エグゼクティブサマリーでコンパクト表示＋詳細は既存レポートへリンク
- 既存レポートへのリンク時は、会議の対象期間等のコンテキストを引き継ぐ

---

## 9. KPI・アクションプランタブの表示

### 9.1 画面イメージ

```
┌─────────────────────────────────────────────────────────────┐
│ [サマリー] [詳細分析] [部門報告] [KPI・アクション]            │
├─────────────────────────────────────────────────────────────┤

【KPI・アクションタブ】

┌─ 部門選択 ─────────────────────────────────────────────────┐
│ [全社] [X事業部] [Y事業部] [Z事業部]                        │
└─────────────────────────────────────────────────────────────┘

┌─ X事業部 KPI状況 ───────────────────────────────────────────┐
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│  │顧客満足度│ │受注件数 │ │リード数 │ │解約率  │          │
│  │  85%    │ │  120件  │ │  350件  │ │  2.1%  │          │
│  │目標:90% │ │目標:100 │ │目標:300 │ │目標:<3%│          │
│  │ ⚠️ -5pt │ │ ✅ +20  │ │ ✅ +50  │ │ ✅ OK  │          │
│  │[詳細→] │ │[詳細→] │ │[詳細→] │ │[詳細→] │          │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘          │
└─────────────────────────────────────────────────────────────┘

┌─ X事業部 アクションプラン進捗 ──────────────────────────────┐
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📋 働き方改革推進                    進捗: ████░░ 65%  │   │
│  │    担当: 田中部長  期限: 2026/09/30                  │   │
│  │    次のマイルストーン: 制度設計完了（7/15）          │   │
│  │                           [ガント→] [カンバン→]    │   │
│  └─────────────────────────────────────────────────────┘   │
│  [+ 会議アクションを追加]                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 10. 今後の検討事項

- [ ] 見込入力画面へのコメント入力UI追加
- [ ] スナップショット取得バッチの詳細設計
- [ ] レポートコンポーネントの config_json 詳細仕様
- [ ] Excel出力/印刷機能の仕様
- [ ] 通知機能（締切リマインド等）の検討

---

## 変更履歴

| 日付 | 変更内容 |
|------|----------|
| 2026-01-15 | 初版作成 |
| 2026-01-15 | 議事録機能追加（meeting_eventsに3カラム追加、meeting_attachmentsに議事録添付対応、画面B5/D8追加） |
